# Arco — stan projektu i handoff (do kontynuacji w nowej sesji)

> Czytaj razem z `CLAUDE.md` (źródło prawdy) i resztą `docs/`. Ten plik mówi: gdzie jesteśmy i co dalej.
> Aktualizacja: 2026-07-08.

## ⭐ STAN NAJNOWSZY (czytaj to pierwsze)
Pracujemy **sprintami** (`docs/sprinty-szczegolowe.md` = żywy plan z podziałem Claude/Ty). Wizja + konkurencja: `docs/roadmap.md`, `docs/konkurencja-hevy.md`. Pamięć: `proactive-architecture-review`, `arco-long-term-vision`.

**Zrobione:** redesign „Athletic" + theme toggle + **Sprint 1** (polish loggera) + **Sprint 2** (ekran po treningu/celebracja + cel tygodniowy + last-set per wiersz) + **Sprint 3** (kalendarz/passa na /history + **anatomiczna heatmapa** na /progress, `react-body-highlighter` zwendorowana do `vendor/`) + **Sprint 4 [Claude]** (`f881bb2` — wspólny `ExerciseBrowser` z filtrami partia/sprzęt/wzorzec dla add+swap; **stoper `timed` z odliczaniem do celu**; mapowania w `lib/exerciseFilters.ts`).

**Zrobione (cd.): Sprint 5 KOMPLETNY** — guidance rule-based (rdzeń wyróżnika): hint progresji rozszerzony (pełny/poniżej zakresu, timed) + balans push/pull + staleness partii (Faza A `e9cb7f1`) + **deload wg stagnacji metryki** (Faza B `1eb6969`). Wszystko w karcie „Wskazówki" na home + loggerze. Progi profilu „standardowego" jako stałe w `lib/guidance.ts` (`GUIDANCE`).

**Zrobione (cd.): Sprint 6 — audyt FBW + nowy zestaw programów.** Audyt (`docs/audyt-fbw.md`) + korekty kategorii (`EXERCISE_CATEGORY_OVERRIDE`) + **6 kuratorowanych programów** (`docs/trainings/` od trenera → seed `ce53b29`, zastąpiły stare 7; mapowanie: `docs/trainings-mapowanie.md`). Aktywny: „Dom z hantlami".

**Zrobione (cd.): S6 KOMPLETNY** — custom ćwiczenie (`dbc8391`): `user_id` na `exercises` + RLS (wzorzec programs), formularz „+ Własne ćwiczenie" w pickerze (typ/sprzęt/partia/wzorzec/opis/zdjęcie → publiczny bucket `exercise-photos`), badge „własne", usuwanie w ⓘ z guardem historii/programu. Akcje zwracają `{error}` zamiast throw (Next maskuje message w prod). **N2 też ZROBIONE** (pkt 1–6, 8–9; #7 reorder opc. nietknięty) — statusy i commity w `plan-sprintow-2026-07.md`.

**⚠️ Incydent 2026-07-02 (patrz pamięć `arco-test-data-cleanup`):** hurtowe `delete from sessions` skasowało realny trening właściciela z 1.07. Odtworzony z PR-ów jako sesja-rekonstrukcja `f2e5386a` (Dzień A + 3 extra, najlepsze serie; snapshot: `backups/pr-snapshot-2026-07-02.csv`). PR-y oryginalne nietknięte.

**Zrobione (cd.): S12 KOMPLETNY (bez „edycji zapisanego treningu” — Refinement [Ty])** — mini-bar „Trening w toku” globalnie (9401708), rep-PRs on-the-fly + hint „rekord przy N powt.” + mikro-celebracja PR (volt-flash+badge, b17aaca), edycja daty sesji (49904ea). Też: Switch w ustawieniach (0bfadfd). **Sync z Notion: rytuał działa** (wpisy S12 na „Do testu [Ty]”).

**Zrobione (cd.): S13 KOMPLETNY** — Muscle Split % (historia+celebracja, `85c2cfa`), delta-karty + nagłówki-interpretacje na /progress (`33538bd`, ton/progi do kalibracji [Ty]), picker: Ostatnio używane + multi-select + 📈 do progresu (`88a8c6e`). Notion zsynchronizowany (Do testu [Ty]).

**Zrobione (cd.): S14 KOMPLETNY** — empty states wg `docs/archive/empty-states-copy.md` (copy zaakceptowane warunkowo, `a555d18`) + skeletony tras i banner offline (`847e25b`). Też: **fix stref czasowych** (`fix(dates)` — klucze dni UTC→lokalne; caveat Vercel-UTC przy N1). Notion zsynchronizowany.

**Zrobione (cd.): S8 + S9-cz.1** — audyt bazy ćwiczeń (`fbb45fc`: baza zdrowa, 5 braków instrukcji załatane, kuracja=Refinement) + audyt zależności (`0332493`: patche minor, smoke naprawione 3/3 — teraz przywracają aktywny program; 5 vuln = wszystkie w next@14 → decyzja majorów [Ty]). Smoke wymagają `ADMIN_PASSWORD=arco1` w env.

**Zrobione (cd.): N3 — RESKIN „ARCO WARM"** (`ec10f19`, decyzja [Ty] 2026-07-04): terracotta #C63F21 + krem + ciepła czerń, **default JASNY**, logo/favicon wpięte, day-pills wg inspo, elevation bez ramek. **Forced-dark loggera ZAWIESZONY** — decyzja po teście [Ty]. Wcześniej: review repo + ikony→lucide (`972a3c9`). Stary „Athletic" tylko w historii gita.

**Zrobione (cd.): Font DM Sans** (`c080db1`) — przy okazji naprawiony brakujący `fontFamily.sans` w Tailwind (font z `next/font` był od startu nieużywany).

**Zrobione (cd.): Paleta Warm v2 + większe logo** (`7910137`, wg `docs/paleta-arco-warm.md` — **źródło prawdy dla tokenów**, nadpisuje opis w N3): canvas **neutralny szary** `#F7F7F7` (nie kremowy), sand/krem `#F6F2ED` tylko jako **powierzchnia brandowa** (hero/celebracja/onboarding, max 1/ekran), logo w headerze powiększone `h-6→h-8`. Rampy grey/rust/sand/stone/ink w `globals.css`.

**Zrobione (cd.): 2 autorskie programy** (`e56a404`) — „Autorski · Siłownia · FBW 2×" + „Autorski · Dom z hantlami · FBW 2×" wpięte w **istniejący** system `programs`/`program_day_slots` (nie nowy schemat — porzucony pomysł równoległego `content_programs`, zob. niżej). Mapowanie na realne `exercise_id`; 3 braki 1:1 rozwiązane swapami **z plików źródłowych autora** (nie zgadywane). Pull-up jako prawdziwy AMRAP (pierwszy taki przypadek w seedzie). Biblioteka: **8 programów**.

**Zmiana procesu (2026-07-05): Sync z Notion NA ŻĄDANIE**, nie automatycznie po paczce — koszt tokenów przy częstych aktualizacjach. Lokalne docs (ten plik + `plan-sprintow`) aktualizuję zawsze; do Notion wypycham dopiero gdy Daniel poprosi. Zaległe wpisy czekają w `docs/notion-sync-queue.md` (Warm v2 + logo, 2 nowe programy).

**Odrzucony pomysł (2026-07-05):** równoległy schemat `content_programs`/`content_sessions`/`content_program_exercises` (Program/Session/ProgramExercise z enumami pattern/target, bez FK do `exercises`) dla contentowego seeda z Markdownu. Właściciel uznał to za nadmiarowe — wystarczył istniejący pipeline. Nie odtwarzać bez wyraźnej nowej decyzji.

**Zrobione (2026-07-11 · XIII) — BUGFIXY Z DOGFOODINGU (feedback #2, `c40efb4`).**
- **Nowy wspólny komponent `components/ui/bottom-sheet.tsx`** (vaul, `handleOnly`+`Drawer.Handle`) — naprawia u źródła konflikt scroll/przeciągnij-zamknij, który dotykał KAŻDEGO sheeta w apce, nie tylko tego jednego zgłoszonego. `ExerciseInfoSheet` i `SwapPanel` (dotąd inline panel bez zamknięcia) przepisane na wspólny komponent.
- **Waga po przecinku** — `input[type=number]` blokował przecinek na poziomie DOM; przejście na `type="text"` z odsprzęgniętym lokalnym stanem tekstu (inaczej React zjadał "22," → "22" w trakcie pisania — number ma na to wyjątek w silniku, text nie).
- **Touch targety w loggerze** — ✓ 40→44px, ✕ pełna 44px hit-area, gap między wierszami serii 4→8px (mechanizm mylenia rzędów).
- **Top bar na dacie** — sprawdzone na wszystkich ekranach, nie reprodukuje się w przeglądarce; prawdopodobnie już pokryte przez wcześniejszy safe-area fix (`bdeb477`) na `<body>` — **do potwierdzenia na telefonie z notchem**.
- Zdjęcie-nakładka po treningu (Strava-style) — świadomie zostawione jako backlog/feature, nie wchodziło w scope bugfixów.
- Zweryfikowane w Preview end-to-end (scroll bez auto-close, X/tło zamykają sheet, "22,5"→DB `22.5`, ✓ 44×44px), tsc+build ✓, smoke 3/3, sesja testowa sprzątnięta po ID. Szczegóły: `docs/feedback-uzytkownikow.md` #2.

**Zrobione (2026-07-11 · XII) — S10 KOMPLETNY (offline correctness + audyt longevity).**
- **Offline-guardy** (`lib/offlineGuard.ts`): `ensureOnline()` wpięty w `SwapPanel.pick`, `ExercisePicker.pick`/`pickMany`, `useSessionMutations.handleSkip` — offline pokazuje czytelny komunikat zamiast cichego/generycznego błędu z catch. Serie NIE dotknięte (mają już outbox z S9-cz.2). Zweryfikowane w Preview z `navigator.onLine=false`: skip pokazał toast i nie zaaplikował zmiany; add-picker nie wysłał POST-a (network log czysty przy offline) — identyczna implementacja jak zweryfikowany wizualnie skip.
- **CSP Report-Only** (`next.config.mjs`): `default-src 'self'` + dozwolone Supabase (connect/img) i hotlink `raw.githubusercontent.com`; nagłówek potwierdzony na `next start`. Enforce dopiero S11, po przejrzeniu realnych naruszeń w DevTools.
- **Checklista longevity 6/6** (pamięć `proactive-architecture-review`): zależności (vendor ✓, audit bez zmian — 5 vuln w next@14), assety (backup 197 MB aktualny), utrata danych (smoke-offline 3/3), migracje (16 lokalnych = 16 remote), sekrety (0 service-role poza scripts/, .env.ops.local posprzątany), przenośność (0 hardkodowanych hex poza uzasadnionym theme-color meta). Pełny raport: `docs/bezpieczenstwo.md` §3 + `docs/sprinty-szczegolowe.md` Sprint 10.
- **RLS re-check** po migracjach kuracji+batch: nadal 11/11 tabel z politykami.
- Commit `43b1792`, sesje testowe sprzątnięte po ID.

**Zrobione (2026-07-10 · XI) — LANDING (etap 1) + NOTION FLUSH I PORZĄDKI.**
- **Landing:** `docs/landing-copy.md` = pełne copy PL wszystkich 9 sekcji w ToV (wariant główny + alternatywa) + specyfikacja eventów PostHog (landing_viewed/waitlist_signup… z `variant` z JEDNEGO źródła — feature flag). `docs/landing-prototyp.html` = działający prototyp retro-analog Warm (krem+ziarno+display DM Sans 900, mockupy minimal-UI jako celowy kontrast, przełącznik podglądu cen A/B 14,99↔9,90, FAQ, RODO-checkbox) — **poligon przed Framerem, otwiera się podwójnym kliknięciem**; zweryfikowany wizualnie sekcja po sekcji. Decyzje [Ty] bez zmian: domena, ESP (rek. MailerLite), plan Framera, founder's note (copy §7 do personalizacji).
- **Notion (na żądanie właściciela):** pełny flush — 17 CREATE + 13 UPDATE; porządki „usuń niepotrzebne" = 7 nieaktualnych wpisów zamkniętych jako Done z adnotacją-dlaczego (nic nie kasowane twardo), 2 re-priorytetyzacje wg wizji v2 (pody ↑, trenerskie ↓). Szczegóły: `docs/notion-sync-queue.md` sekcja „Ostatni flush: 2026-07-10". Kolejka PUSTA.

**Zrobione (2026-07-10 · X) — WDROŻENIE KURACJI + S9-cz.2 KOMPLETNY (paczki 3–5).**
- **Wdrożenie lokalne [za Ciebie]:** obie migracje (`20260708100000` kuracja + `20260708120000` batch) + seed → **905 ćwiczeń / 140 hidden / 8 programów bez „Autorski"** · aktywny program przywrócony po re-seedzie (best-guess: „Intermediate · Dom z hantlami · FBW 2×" — **zweryfikuj, czy to ten**, re-seed czyści `user_active_program` przez CASCADE) · testy w Preview: picker (chipy ukrywają stretching/cardio, search „stretch" znajduje), swap dla hip thrust = hinge (nie pressy), nowe ćwiczenia (Bulgarian/Nordic/Pike) z placeholderem Warm + instrukcjami · batch „poprzednio" **zweryfikowany 8/8 identycznie ze starą funkcją** (obie 0 dla nowych slotów — po re-seedzie sloty mają nowe UUID, więc „poprzednio" per-slot startuje od zera; to artefakt KAŻDEGO re-seedu, nie batcha; matching po exercise_id działa dla freestyle) · build + smoke 3/3 · sesje testowe sprzątnięte po ID.
- **Commity rozdzielone:** `7a32391` feat(exercises) kuracja+programy · `a0fdecf` perf(s9.2) paczki 1–2+fixy · `be35f9a` docs sesja 2026-07-08 · `fcf8669` refactor paczka 3 · `dbce3fa` refactor paczka 4. **⚠️ PUSH WSTRZYMANY** — patrz „Następny".
- **Paczka 3 — split Logger.tsx (768→249):** `ExerciseCard` + `SetRow` (memo z komparatorami pomijającymi funkcje — tap ✓ renderuje 1 kartę/1 wiersz) + `useRestTimer` + `useSessionOutbox` (grunt pod S10) + `useSessionMutations` (inwariant handlerów udokumentowany) + `finish.ts`. Pełny test przepływu w Preview (seria 20kg×10, ✓+rest-bar, superset, notatka, RPE, pomiń/przywróć, swap-panel, zapis przez outbox do DB).
- **Paczka 4 — split /progress (474→~100):** `stats.ts` (dane, 4 grupy zapytań równolegle) + `sections.tsx` + **heatmapa przez `next/dynamic`** (First Load 98,7 kB). **Cache detalu ćwiczenia: sprawdzone i ODRZUCONE** — RLS `exercises_select` wymaga authenticated; anon-read albo service-role = nowa powierzchnia za 1 indeksowany SELECT (szczegóły `optymalizacja.md` P3).
- **Paczka 5 — domknięcie:** `npm audit` bez zmian (5 vuln, wszystkie next@14 → decyzja majorów [Ty]) · **Lighthouse przed→po** (mobile/throttling/prod): home 94→95 · /progress 94→95 (LCP 3,1→2,9 s) · logger 95→95 — budżet ≥90 trzymany · `audyt-kodu-zaleznosci.md` (część 2 = DONE) i `optymalizacja.md` (findings ✅) zaktualizowane.

**Zrobione (2026-07-08 · I) — PRODUKT: kuracja bazy ćwiczeń + programy.** Baza 873→**905** (+32 współczesne ćwiczenia z placeholderem SVG; Pike Push-Up dla programu bodyweight), kolumna `exercises.hidden` (migracja `20260708100000` + `deriveHidden` w seedzie): **140 ukrytych** (stretching/cardio + 8 przestarzałych behind-the-neck/guillotine/twist) — hidden filtruje browse/chipy i swap engine, **search znajduje wszystko**; whitelisty: programy/historia/PR-y/custom + 5 błędnie skategoryzowanych. Programy: przywrócony oryginalny zamysł trenera z `docs/trainings/` (Bulgarian SS ×5, Chest-Supported Row ×3, DB Hip Thrust ×2, Nordic, Hollow Hold, Ab Wheel, Hanging Knee Raise, Close-Grip Pulldown…), nazwy bez „Autorski" → „Intermediate · Siłownia/Dom z hantlami · FBW 2×"; `movement_pattern→hinge` dla hip thrustów/pull-through/KB swings. Weryfikacja: 73 ID programów istnieją, tsc ✓. Szczegóły: `audyt-bazy-cwiczen.md` §5. **Do zrobienia [Ty]: migracja + `npm run seed` + test pickera/swap w Preview (prompt wdrożeniowy dostarczony w rozmowie). Zmiany NIEZACOMMITOWANE** (rozdzielić od wcześniejszych niezacommitowanych docs).

**Nowe (2026-07-08 · II) — KANON: WIZJA v2 + FUNDAMENTY WYKONAWCZE.** **`wizja-i-plan-produktu-v2.md` = kanon nadrzędny** (geneza: audyt biznesowy, w `archive/`): warstwa trenerska ODŁOŻONA, **pody = silnik wzrostu** (fast-follow po launchu; widzą check-in+passę, NIE logi), model hybrydowy limity+wartość (free: 2 programy/10 custom/12 tyg. historii; Coach 14,99/99 zł; reverse trial 21 dni; zasady Z1–Z3), dwie fale konwersji, Kroki 0–5 + bramki B1–B4, art direction retro-analog Warm (ikony clay, nie metalik). Rewizje §8 NANIESIONE w: monetyzacja / konkurencja-hevy / roadmap / CLAUDE.md / sprinty-szczegolowe. Fundamenty pod wykonanie (wszystko 2026-07-08, po refinement — 11 poprawek): **`audyt-kodu-pod-wizje-v2.md`** (wizja = ~80% dobudowa; wycena 13–20 tyg.; S9-cz.2 PRZED Krokiem 3; decyzje [Ty] §4), **`projekt-schematu-subs-consents-pods.md`** (get_plan() z trialem bez tabel, consents stan+audyt, pody z RLS security-definer, anty-spam nudge, grace 7 dni; 7 decyzji [Ty] §6), **`instrumentacja-metryk.md` + `lib/analytics.ts`** (taksonomia pod B1–B4, PostHog EU rekomendowany — decyzja [Ty]; wire w S11), **`scenariusz-h2.md`** (70 min/os.: first-click Z0 + Z1–Z5 + WTP/Van Westendorp + moduł Radka; progi B1), **`concierge-test-podow.md`** (teza Radka na WhatsApp PRZED budową Kroku 4), **`kalendarz-wykonawczy.md`** (launch = **okno 2–8 sty 2027**; scenariusze 10/15 h/tydz. z buforem; reguły poślizgu; kancelaria+Stripe umówić w sierpniu). ⚠️ Zweryfikowano: **Hevy MA polski** → klin bez języka (korekta w konkurencja-hevy; nanieść w wizji przy rewizji). Ocena planu po domknięciu: **8,5/10**.

**Nowe (2026-07-11 · 2) — AUDYT UX LOGGERA (`docs/audyt-loggera.md`, do akceptacji [Ty]).** Werdykt: mechanika serii = klasa Hevy/Strong z przewagami (hint progresji, auto-przerwa per slot, offline badge); problem systemowy = **6 zawsze-widocznych akcji drugorzędnych na każdej karcie** (Podmień/Pomiń/Superset/przerwa±/notatka/RPE — szum × liczba ćwiczeń) + „poprzednio" w 3 miejscach naraz. Rekomendacje: **R1 menu ⋯ per karta** (wszystkie akcje drugorzędne, 2 tapy) + ⋯ sesji (tam „Usuń sesję" z dołu main), **R2 jedno „poprzednio"** (per-wiersz ↺+placeholder, agregat z nagłówka wypada), R3 targety (W-toggle 28px→44), **R4 finish-sheet zamiast natywnego confirm()** (podsumowanie tylko przy niezaliczonych), R5 kosmetyka copy/affordance. Świadomie bez zmian: ✓44, tap-to-fill, hint, superset-border, rest-bar. Wycena: ~3 wieczory („logger polish"), zdąży przed H2 (Z2 przetestuje nową kartę). Makiety przed/po w rozmowie. **Uzupełnienie (pytanie [Ty] o superset, §5 audytu):** łączenie mechanicznie solidne (grupy, giant sety, sprzątanie osieroconych), ALE **F10-P1: superset jest tylko wizualny — przerwa odpala się także MIĘDZY partnerami pary** (apka namawia do złamania metody, którą pozwala zadeklarować); F12: intuicja „z kolejnym" trafna (decyzja zapada przy A, patrząc w dół). Rekomendacje R6: (a) przerwa świadoma grupy — po ✓ hint „→ teraz: partner", przerwa dopiero po ostatnim ogniwie rundy, label „Przerwa po supersecie"; (b) w menu ⋯ „Połącz w superset" z wyborem partnera (default = następny) — kierunko-agnostycznie zamiast flipowania kierunku; (c) wizualne sklejenie pary (P3). **+R7 (pytanie [Ty] = powrót N2#7): reorder ćwiczeń w sesji** — ⋯ „Przenieś wyżej/niżej" (`position` już jest w schemacie, zero migracji; bez dnd-liba — zasada wagi paczek; grupa supersetu przesuwa się jako całość; offline-guard jak swap; toast edukacyjny po dodaniu z pickera); v2 tryb uchwytów tylko jeśli dogfooding pokaże ból. Sprint „logger polish" = ~3–4 wieczory, audyt KOMPLETNY (12 findings + R1–R7), czeka akcept [Ty].

**Nowe (2026-07-11) — REDESIGN HOME: koncept DO AKCEPTACJI (`docs/redesign-home.md` + makieta w rozmowie).** Diagnoza z kodu: nie liczba bloków, tylko **3 konkurujące paradygmaty startu** (hero + lista wszystkich dni w sekcji Program + freestyle) — feedback #1 potwierdzony strukturalnie. Wzorce: Hevy (segregacja jobs, 1 start/karta, zarządzanie poza ekranem startu) + coaching-app (płomienie dni, hero z meta). Decyzje: (1) **FlameWeek** — day-pills → 7 płomieni (done=terracotta, dziś=obrys ink z pulsem, minione-bez-treningu=neutralne wygaszone, NIE „przerwane" — ToV; animacja zapłonu po treningu jako moment); (2) **sugerowany trening = hero, zostaje i rośnie** (jedyna akcja główna; alternatywy — Zobacz/Inny dzień-sheet/Freestyle — jako tekst-linki w stopce hero); (3) **Wskazówki → GuidanceChip** (1 linia najwyższego priorytetu, tap→sheet; „Wszystko na torze" zminiaturyzowane; pełny widok docelowo w /progress); (4) wypada: sekcja Program (→sheet+/programs), sekcja freestyle, powitanie (rekomendacja), logout z headera (→settings). 7 bloków → 3 sekcje. Wdrożenie: F1 hierarchia (1 wieczór) → F2 FlameWeek (1 wieczór; lucide Flame, docelowo własny glif) → F3: **Z0 w scenariuszu H2 zaktualizowane — testuje NOWY home**. **Decyzje [Ty] 2026-07-11 (eksploracja na makietach):** koncept ZAAKCEPTOWANY („bardzo mi się podoba"); **hero = V4: BIAŁY kafel + hierarchia typografią + jedyne rust-CTA** (sand WYCHODZI z codziennego UI — rewizja naniesiona w `paleta-arco-warm.md` i CLAUDE.md; diagnoza: sand na canvasie obok bieli = „przybrudzony biały", ~1.03:1); **powitanie „Cześć, {imię}" ZOSTAJE** („miło przeczytać swoje imię"); pusty stan §3.6 zaakceptowany. Pozostało otwarte: neutralne wygaszone płomienie (rekomendacja stoi) + własny glif płomienia (tor assetów). **Gotowe do F1** (kod: tu albo Claude Code). **Uzupełnienie (§3.6, pytanie [Ty]): pusty stan = progressive disclosure** — wariant A (po onboardingu): hero z KONKRETNYM sugerowanym programem i CTA „Aktywuj i zacznij Dzień A" (1 tap do pierwszej wartości); wariant B (skip): hero „Zacznij od planu" + 2 ciche kafle à la Hevy (Ułóż własny / Freestyle — zdegradowane wizualnie, hierarchia zostaje); **FlameWeek ukryty do 1. ukończonego treningu** — debiutuje animacją zapłonu pierwszego płomienia po celebracji (moment); ekran rośnie razem z userem: hero → +FlameWeek → +GuidanceChip.

**Zrobione (2026-07-11 · 3) — SPRINT HOME+LOGGER KOMPLETNY (F1→F2, R1→R7 z `audyt-loggera.md` §7 i `redesign-home.md`).** Oba briefy z 2026-07-11 wdrożone w całości, kolejność jak w brifie (home przed loggerem — home testowany w Z0 scenariusza H2, logger w Z2), commit po każdej paczce, tsc+build+smoke po każdej, dane testowe sprzątane po ID sesji.

- **F1 (`e9596b5`) + F2 (`5550739`) — redesign home.** 7 bloków → 3 sekcje: hero (biały kafel, V4) z jedynym rust-CTA „Start", stopka hero = 3 tekst-linki (Zobacz ćwiczenia / „Inny dzień" sheet gdy program ma >1 dzień / Freestyle), FlameWeek zastępuje day-pills (7 płomieni, ukryty do 1. treningu, animacja zapłonu przez `?trained=1` z ekranu celebracji), GuidanceChip (1 linia + sheet). Logout przeniesiony do `/settings`. **Flaga [Ty]:** wariant A pustego stanu (§3.6, hero z konkretnym sugerowanym programem po onboardingu) NIE wdrożony — wymaga persystencji poziom/środowisko z `WelcomeOverlay` do `user_settings`, czego nie ma dziś w schemacie; zaimplementowany tylko wariant B. „Ułóż własny" z wariantu B pominięty — brak route'a buildera w kodzie, link prowadziłby na 404.
- **R1+R2 (`936ba94` docs, `0301394` kod) — menu ⋯ per karta + ⋯ sesji.** 6 zawsze-widocznych akcji (Podmień/Pomiń/Superset/Przerwa/notatka/RPE) przeniesione do `ExerciseCardMenu` (BottomSheet, 2 tapy); karta = nazwa+ⓘ+cel+hint+serie. „Usuń sesję" przeniesione z dołu main do ⋯ sesji w headerze. Agregat „Poprzednio" z nagłówka karty wypadł (per-wiersz ↺+placeholder wystarcza). Zero nowych zależności — reużyty `BottomSheet`, nie dropdown-menu.
- **R6a+R6b (`275e431`) — superset dostał świadomą przerwę + kierunko-agnostyczne łączenie.** `maybeStartRest` w `useSessionMutations.ts`: po ✓ serii z grupy — jeśli partner ma mniej zaliczonych serii tej rundy, przerwa NIE startuje (toast „→ teraz: {partner}"), rusza dopiero po ostatnim ogniwie z labelem „Przerwa po supersecie". „Superset z poprzednim" → „Połącz w superset" z pickerem DOWOLNEGO partnera sesji (następny podświetlony jako sugerowany); `linkWithPartner(i, partnerIndex)` generalizuje stary `linkWithPrevious`. W tej samej paczce: wyrównanie nagłówka kolumn KG/POWT. do realnej szerokości ✓/✕ (były rozjechane), wiersz serii do `h-11`, truncate długiej nazwy z ⓘ+badge zawsze obok, top bar (tytuł = nazwa dnia zamiast ucinanego „program · dzień", „Zakończ" outline zamiast wypełniony rust — jedyny wypełniony akcent to logowanie serii).
- **R7 (`416cb98`) — reorder ⋯ „Przenieś wyżej/niżej".** `reorderExercise` w `app/actions/sets.ts`: dzieli `session_exercises` na bloki (sąsiednie wiersze tej samej grupy SS = jeden blok), zamienia blok z sąsiadem, przenumerowuje `position`. Grupa SS przesuwa się jako całość — zweryfikowane w Preview. Offline-guard jak swap. Jednorazowy toast po dodaniu z pickera („Dodane na końcu — przenieś ⋯…"), flaga w `localStorage`.
- **R3 (`6bfb98a`) — touch targety.** W-toggle i ✕ na pełne `h-11 w-11` (były węższe niż norma 44px); nagłówek kolumn zaktualizowany zgodnie.
- **R4 (`f696a1c`) — finish-sheet.** Natywny `confirm()` przy niezaliczonych seriach zastąpiony `FinishSheet` (podsumowanie „✓ N serii · M niezaliczonych · X min" + „Zakończ trening"/„Wróć do treningu"). Czysta sesja kończy się od razu, bez sheeta — bez zmiany zachowania.
- **R5 (`441f261`) — copy/affordance.** Nazwa ćwiczenia = czysty tekst (nie trigger), ⓘ jedynym sposobem otwarcia „jak wykonać" (hover-underline nie działał na dotyku). Copy pominiętego ćwiczenia bez zagnieżdżonych cudzysłowów, wskazuje na ⋯ (gdzie akcja realnie żyje po R1). `aria-label` obok `title=` na W-toggle (tooltip niedostępny na dotyku).

**Odkryty po drodze, ważny dla kolejnych sesji:** Preview lokalnie serwuje przez `next start` (build produkcyjny), nie `next dev` — **źródło musi być zbudowane (`npm run build`) PRZED każdą weryfikacją wizualną**, samo `preview_stop`+`preview_start` nie odświeży kodu (pamięć `arco-preview-requires-rebuild`).

**Nie ruszane (świadomie poza zakresem tej paczki):** Krok 2–4 wizji, majory, R6c (wizualne sklejenie pary supersetu, P3), reorder v2 z uchwytami (P3, tylko jeśli dogfooding pokaże ból ze strzałkami).

**Zrobione (2026-07-08 · IX) — S9-cz.2 PACZKI 1–2 (plan zaakceptowany [Ty]).** **Paczka 1 — batch „poprzednio":** migracja `20260708120000` z `previous_session_sets_batch(p_session)` (LATERAL na istniejącej funkcji = poprawność z konstrukcji; security invoker, grant authenticated) + `session/[id]/page.tsx` na 1 RPC zamiast N + typ w `database.types.ts`; tsc ✓. **Paczka 2 — paginacja historii:** lista 20 + kursor `?before=` (limit 21 = detekcja starszych) + „Pokaż starsze/Najnowsze"; **kalendarz+passa z osobnego lekkiego zapytania po datach** (pełny obraz niezależnie od strony); empty states zachowane + nowy „to już wszystko"; tsc ✓. **Zostały paczki 3–5** (split Logger + memo, split progress + dynamic heatmapa + cache detalu, domknięcie z Lighthouse) — plan w `plan-s9-cz2.md`. ⚠️ Migracja batch stackuje się za migracją kuracji — **wdrożenie [Ty] obejmie obie naraz** (`migrations up` + seed + test loggera: „poprzednio" per wiersz musi wyglądać identycznie).

**Nowe (2026-07-08 · VIII) — SZYBKIE FIXY PERF + PLAN S9-cz.2.** Załatane od ręki (tsc ✓): wymiary+lazy+decoding na obrazach (exercise/[id], InfoSheet, Browser, body — CLS z głowy) i wąski select w `substitute.ts` (koniec ciągnięcia instructions+images przy swapie); korekta przeglądu: `select("*")` na session_sets zostaje (lekkie, używane). **`docs/plan-s9-cz2.md` = plan sprintu DO AKCEPTACJI [Ty]:** 5 paczek — batch „poprzednio" (1 RPC zamiast N, wymaga migracji → po wdrożeniu kuracji), paginacja historii (20 + starsze; kalendarz osobno), split Logger.tsx (<250 linii + useRestTimer/useSessionOutbox — grunt pod S10 + memo na wierszach), split progress + dynamic heatmapa + cache detalu ćwiczenia, domknięcie (audit, Lighthouse przed/po). Wycena ~3–5 wieczorów; poza zakresem: reorder, wizual, majory. Kolejność: kuracja [Ty] → S9-cz.2 → S10.

**Nowe (2026-07-08 · VII) — OPTYMALIZACJA: budżety + zasady + przegląd (`docs/optymalizacja.md`).** Sprzęt odniesienia: średni Android/4G (obietnica Z1 = frictionless na siłowni, nie na flagowcu). Budżety: LCP <2,5 s, **tap ✓ w loggerze <100 ms**, JS <200 KB gz/trasa, obraz <100 KB, ≤4 zapytania/widok, Lighthouse ≥90 (home/logger) przy bramkach. Przegląd repo: stan dobry (lean deps bez chart-libów, 13 indeksów z gin, skeletony, debounce, optymistyczny logger, force-dynamic świadome); findings: **P1 obrazy surowym `<img>` bez wymiarów** (fix tani teraz: width/height+lazy; docelowo self-host w rozmiarach = S11, spina się z AI-gradingiem top 200), P1 N+1 „poprzednio"+brak paginacji historii (=S9-cz.2, już w planie), P2 `select("*")` ×2, split Logger/progress + memo + dynamic import heatmapy (S9-cz.2), P3 web vitals z pola przez PostHog (Faza 1). **Wniosek: 80% długu perf już siedzi w S9-cz.2 — dokument dodaje budżety i kolejność, nie nową robotę.** 10 zasad twardych wpiętych do CLAUDE.md, checklist §4 dokleja się do checklisty designu.

**Nowe (2026-07-08 · VI) — BEZPIECZEŃSTWO: zasady + przegląd (`docs/bezpieczenstwo.md`).** Przegląd wykonany NA REPO — stan dobry: **RLS 11/11 tabel**, body-photos prywatny z folder-RLS, auth-guard we wszystkich akcjach, middleware chroni trasy, zero service-role/dangerouslySetInnerHTML w apce, .env poza gitem, upload z limitami. **Załatane od ręki (P1): security headers w `next.config.mjs`** (nosniff, X-Frame DENY, Referrer-Policy, Permissions-Policy; HSTS = Vercel) — **zweryfikować przy najbliższym buildzie [Ty]**. Findings otwarte: CSP Report-Only (S10) → enforce (S11) · decyzja [Ty] o publicznym `exercise-photos` (Krok 2) · rate limiting (Krok 2/4) · majory next (S11) · plan backupów+test restore (S11). 10 zasad twardych (m.in. RLS w tej samej migracji + test wielokontowy, sekrety przez pliki jednorazowe, żadnych hurtowych DELETE — pamięć incydentu 2026-07-02) wpiętych do CLAUDE.md; checklisty per bramka (S10/S11/Krok 2/Krok 4); proces incydentów RODO-ready (UODO ≤72 h od Kroku 2), 2FA wszędzie [Ty — sprawdź], security@ od launchu.

**Nowe (2026-07-08 · V) — WYTYCZNE DESIGNU: HIG adaptowane pod PWA (`docs/wytyczne-designu.md`, decyzja [Ty]).** Strategia: HIG jako poprzeczka jakości, nie kopiowanie iOS (PWA + Android-majority PL). 12 zasad twardych zmapowanych na stan Arco (większość już spełniona: 44px, safe-area, sheets, empty states, dark semantyczny; ⚠️ zoom i dark-auto = znane P0 z usability-audit), lista czego NIE bierzemy (SF/Liquid Glass, iOS-wzorce łamiące Androida — back gesture, Web Share API), **checklist 10 punktów per nowy ekran wpięty do Definition of Done w CLAUDE.md**, hierarchia konfliktów: nasz system → wytyczne → odruchy platformy. Feedback #1 (home) sklasyfikowany jako naruszenie zasady hierarchii — checklist pkt 10 pilnuje, żeby nie dokładać do home bez zabierania.

**Nowe (2026-07-08 · IV) — RESEARCH: powiadomienia + paywalle → wnioski naniesione.** Transkrypcje 2 filmów (Enrico: inżynieria powiadomień; Mobbin: badanie 2850 paywalli) w `docs/inspiracje/` + **synteza `inspiracje/wnioski-dla-arco.md`** (bierzemy/odrzucamy/parkujemy). Naniesione: **schemat §4** — zasady dostarczania nudge (quiet hours 22–7, okno wg typowej pory treningu odbiorcy, konkret w pierwszych słowach bo AI OS-ów streszcza push, jeden typ nudge'a zero marketingu); **ToV §3** — wzorzec „Start triala d0 = timeline Blinkist" (nasz as: bez karty), subtitle „bez zobowiązań / anuluj w 2 tapy", nowe zakazy: groźba utraty passy, curiosity gap w podach, fake urgency; **audyt-kodu §Krok 3** — 6 wytycznych paywalla (flow diagnoza→obietnica→cena, timeline zamiast odliczania, 2 ceny+tabela, exit-intent z miesięcznym, **anulowanie ≤2 tapy**, testy radykalnych wariantów przez flags); **landing §3** — pricing copy; **roadmap backlog** — własne przypomnienie treningowe (opt-in, timing z mediany started_at, po Kroku 4). Kluczowa decyzja świadoma: **odrzucamy skuteczne techniki łamiące ToV** (loss-aversion na passie, spin-the-wheel) — uczciwość jako moat, na piśmie.

**Nowe (2026-07-08 · III) — MARKA I MARKETING (komplet przed landingiem).** **`tone-of-voice.md`** (kumpel z siłowni, dwie głośności, prompt do sesji copy §4), **`prompt-ikony-3d-clay.md`** (hybryda: 3dicons.co CC0 + AI-clay dla gym-setu; system materiałów Warm), **`strategia-marketingowa.md`** (pozycjonowanie „księga vs kumpel-trener", 3 persony, messaging house, lejek AARRR, kampania launchowa 4 fazy pod okno styczniowe, 10 artykułów K1, gwiazda północna: **userzy ≥4 treningi/mies.**), **`plan-dystrybucji.md`** (K1 content startuje z landingiem; ≥20% czasu na dystrybucję po launchu; zasada 10:1; cel 300–500 na liście), **`landing-plan.md`** (**Framer TAK** z 3 warunkami: domena docelowa, ESP UE zamiast Framer Forms, PostHog od zera; 9 sekcji; test cenowy A/B 14,99 vs 9,90), **`baza-contentu-instagram.md`** (38 postów, 7 rubryk stories, 4 szablony, batch 1 wieczór/mies.), **`feedback-uzytkownikow.md`** (NOWY log; #1: „za dużo na home" → first-click w H2, decyzja po testach). Backlog roadmapy +3 pomysły [Ty]: Monthly/Yearly recap, retro display typografia, (landing = ich poligon). Decyzje [Ty] zebrane w §§ końcowych każdego doku.

**Nowe (2026-07-06): STRATEGIA MONETYZACJI — kanon w `docs/monetyzacja.md`.** *(⚠️ wpis historyczny — 2026-07-08 nadpisany przez wizję v2: trenerska ODŁOŻONA, model hybrydowy — patrz wpis II wyżej.)* Decyzja kierunkowa właściciela: PL-first z opcją ekspansji; marketplace programów odrzucony. Etapy: (1) consumer premium „Arco Coach" po H2, (2) **warstwa trenerska = docelowy silnik przychodowy** (nowa w kanonie — uwzględniać przy decyzjach architektonicznych: relacja trener↔podopieczny, nie budować/nie blokować), (3) gate EN/B2B wellness. Roadmap H4 zaktualizowany (monetyzacja rozprzęgnięta od natywu/socialu; PWA+Stripe = atut, bez 30% App Store). R3 ROZSTRZYGNIĘTE (2026-07-06): guidance-lite free / guidance-full paid; hard paywall odpada (zabija bazę pod trenerów i community), przy etapie 1 rozważyć reverse trial. Otwarte [Ty]: R2 pricing (kotwica Hevy $2.99/mc — głębsza analiza przed etapem 1). Rot dokumentacji (R6) sprzątnięty: kickboxing/volt/plate-calc oznaczone w briefie, roadmapie, sprintach i setup-local; 11 skonsumowanych doków → `docs/archive/`, duplikaty z roota i `trainings.zip` usunięte. NIE zmienia scope'u H1–H3 ani bieżących sprintów.

**Zrobione (cd.): N1 — Deploy-lite KOMPLETNY (2026-07-05, `8992b56`).** Apka na produkcyjnym HTTPS: **https://arco-olive.vercel.app**. GitHub (`danielm1212/arco`, push przez SSH — klucz był passphrase-protected, odblokowany przez `ssh-add` w terminalu właściciela) · Supabase cloud (region **eu-central-1/Frankfurt**, potwierdzony zgodnie z audytem technologicznym) — link, 14 migracji, seed (873 ćwiczeń, 8 programów), bootstrap konta · Vercel (`arcoapp/arco`, auto-deploy z GitHub, region funkcji **fra1** — kolokacja z bazą, `vercel.json`) — 5 zmiennych prod, **`ADMIN_PASSWORD` świeżo wygenerowane** (nie dev-hasło, zapisane u właściciela). Ops: sekrety przechodziły przez gitignorowany `.env.ops.local` (usunięty po zakończeniu), nigdy przez literały w komendach bash. Napotkany i obalony problem: `git push` padał na `pack-objects died of signal 10` (mmap timeout w sandboxie) — obejście: `git -c pack.threads=1 -c pack.windowMemory=10m push`. Notion zsynchronizowany od razu (3 wpisy → „Do testu [Ty]": deploy sam, wake lock, koniec przerwy) — **zanim zauważono zmianę zasady sync na na-żądanie** (patrz niżej); dalej stosuję nową zasadę.

**✅ REMOTE DEPLOY WYKONANY (2026-07-10, po `supabase login` właściciela).** Kolejność zachowana: migracje remote (obie, `migration list` potwierdza) → seed remote (**905 ćwiczeń / 140 hidden / 8 programów / 0 „Autorski"** — zweryfikowane przez REST count) → aktywny program przywrócony (Intermediate · Dom z hantlami · FBW 2×) → `git push` (`bdeb477..352361f`, 6 commitów) → Vercel deploy potwierdzony (nowe security headers live: nosniff/X-Frame DENY/Referrer/Permissions-Policy + HTTP 200). Ops: service_role tylko w env pojedynczych procesów (nie zapisany na dysku). **Do testu [Ty] na telefonie:** picker/swap po kuracji, „poprzednio" w loggerze, 2 programy FBW 2×, teraz też offline-guardy (spróbuj podmienić/dodać/pominąć ćwiczenie w trybie samolotowym). **S10 zrobiony (patrz wpis XII) → następny: S11-domknięcie (launch gate)**, ale ten wymaga [Ty]: decyzja o majorach next/React/Tailwind/TS (5 vuln czeka od S9), plan backupów+test restore, CSP enforce. Równolegle [Ty]: decyzje z doków 2026-07-08 (PostHog, domena/ESP/Framer, budżet czasu 10 vs 15 h/tydz., schemat §6, persony/hasło ze strategii §9) + w sierpniu umówić kancelarię i Stripe activation (kalendarz §4). Otwarte [Ty] (starsze): test N1 na telefonie (wake lock/przerwa/PWA), test Warm v2 na telefonie. Landing (etap 1, copy+prototyp) świadomie odłożony — „kiedyś dopracujemy" (decyzja właściciela 2026-07-10). **Kolejka Notion: pusta od pełnego flusha 2026-07-10** (`docs/notion-sync-queue.md`). Mapa całej dokumentacji: **`docs/README.md`**.

**⚠️ Sesja niejasnego pochodzenia w bazie (2026-07-05, `33acba2e...`):** przy weryfikacji zauważona zakończona sesja z ćwiczeniami „Dzień B" i niekompletnymi seriami — może być realny test właściciela na telefonie (serwer LAN chodził cały dzień). **Nie usunięta** — czeka na decyzję właściciela, czy to jego dane czy coś do wyczyszczenia.

**Nowe (2026-07-02): analiza UX Hevy** — `docs/konkurencja-hevy-ux.md` (ekrany + warstwa funkcjonalna, matryca #1–#30). Wynik: sprinty **S12–S14** w `sprinty-szczegolowe.md` (sesja globalna + rep-PRs · postępy-lustro + picker · empty states), punkty 8–9 dopisane do N2, 14 wpisów w Notion „Baza pomysłów" (Backlog). Kolejność wpięcia S12–S14 — propozycja w `plan-sprintow-2026-07.md`, czeka na [Ty].

**⚠️ NOWA KOLEJNOŚĆ SPRINTÓW (2026-07-02):** po S6 wchodzi **`docs/plan-sprintow-2026-07.md`** — re-priorytetyzacja po audycie backlogu właściciela (Notion „ARCO — Baza pomysłów”): N1 deploy-lite HTTPS (wake lock/wibracje to artefakty HTTP, nie bugi kodu) → N2 paczka UX z notatek → decyzja wizualna (gate toru assetów) → S7 (+imię w onboardingu) → S8–S11. Czytaj ten plik przed planowaniem kolejnego sprintu.
> Uwaga: re-seed kasuje programy z `user_id=null` (CASCADE czyści `user_active_program`) — po `npm run seed` ustaw aktywny program na nowo.
> Otwarta sprawa (Twoja): po realnym użyciu guidance — czy progi/agresywność OK (kalibracja `GUIDANCE`), zanim ew. per-user.
> Ops: lokalny LAN IP zmienił się na **192.168.100.53** — `.env.local` (gitignore) zaktualizowany. Stary `.16` był down. Sprawdzaj `ipconfig getifaddr en0` po zmianie sieci.

**Strategia:** wyróżnik vs Hevy = frictionless logging + **rule-based guidance** (jawne reguły, NIE AI) + **kameralny social** (pody + reakcje/nudge, zero komentarzy) — `docs/konkurencja-hevy.md`. Kickboxing porzucony.

**Operacyjnie (ważne):** jeden `npm run build` na raz, **zatrzymaj Claude Preview przed buildem** (równoległy psuje `.next`); login do Preview: gdy formularz nie hydratuje (submit leci jako natywny GET), fallback = **cookie auth**: w `preview_eval` fetch do GoTrue (`/auth/v1/token?grant_type=password`, anon key z bundla) → `document.cookie='sb-192-auth-token=base64-'+base64url(JSON sesji)` → nawigacja (cookie ustawiaj będąc już na `http://localhost:3000`, nie na `data:`).

**⚠️ SPRZĄTANIE DANYCH TESTOWYCH — NOWA PROCEDURA (od 2026-07-02, po incydencie):** apka jest w REALNYM użyciu na telefonie — **NIGDY hurtowe `delete from sessions`**. Kasuj wyłącznie po ID sesji utworzonych w teście (`delete from sessions where id in ('...')`), a przed kasowaniem sprawdź `select id, started_at, finished_at from sessions order by started_at` czy nie ma tam realnych treningów. `recompute_personal_records()` przez `docker exec` (superuser) **nie działa** (`auth.uid()` = null — funkcja cicho nic nie robi); PR-y przelicza apka przy `finishSession`, a ręcznie tylko z tokenem usera przez API.

---

## Redesign wizualny „Arco Athletic" (zrobione)
Po researchu Mobbin + audycie: stary UI był „default shadcn". Kierunek (decyzje właściciela): **akcent volt/lime-green**, **dark logger jako focus mode**, bento + elewacja zamiast 1px ramek, liczby jako bohater. Architektura tokenów bez zmian (primitive→semantic), tylko wartości/kompozycja. Pamięć: `arco-visual-redesign`.

**Zrobione (na `main`):**
- `a6d0a8b` Foundation — tokeny volt (`--arco-volt-*`), tinted canvas, `--radius-xl`, `--shadow-sm/md`, `--volt`/`--volt-foreground`. Reguła WCAG: **volt jako tekst tylko w wersji -600** (5.05:1); jasny volt-400 wyłącznie jako fill z ciemnym tekstem. Light=volt-600, dark=bright volt-400.
- `e36c9d7` Home — bento (rounded-xl + shadow), day-pills z ringiem „dziś", bright-volt hero „Sugerowane dziś", streak-pill. Zweryfikowane Preview light+dark.
- `d3df526` Spójność hubów — Postępy/Historia/Ciało/detale: karty top-level `rounded-xl shadow-sm`, wiersze zagnieżdżone `bg-muted`, staty jako bento.

**Zrobione (cd.):**
- `95a44ed` **Dark logger (focus mode)** — ekran sesji wymuszony `.dark`, bright volt na zaliczonej serii + „Zakończ", karty rounded-xl. **Kluczowy fix tokenów:** L3-bridge shadcn re-deklarowany w `.dark` (bez tego `var()` w moście z `:root` nie re-resolvuje się w poddrzewie `.dark` → jasne tła mimo ciemnej semantyki). Zweryfikowane Preview.

**Plan `docs/plan-theme-i-naprawy.md` — A/B/C ZROBIONE:**
- `a84e37e` **A** theme toggle 3-stan (jasny/ciemny/system, default dark, `next-themes`+localStorage; usunięto `@media prefers-color-scheme`; logger zawsze ciemny). Uwaga: klasa fontu jest na `<body>` (nie `<html>`), inaczej React zdejmuje klasę motywu przy hydracji.
- `6dc6925` **B** walidacja inputów (`clampNum`+`LIMITS` w `lib/format.ts`; koniec buga 2222 kg).
- `f1b2c4e` **C** odchudzone steppery ± w wierszu serii.

**Zostaje Faza D (funkcje, osobno, po kolei):** „last set" inline per wiersz · kalendarz/streak · heatmapa-sylwetka (mamy `sets-per-muscle`).

## Kierunki produktowe — brief v0.3 (`docs/build-brief-v0.3-addendum.md`)
Analiza jasności briefu vs audyty + konkurencja. Brief v0.2 zostaje źródłem prawdy; addendum nadpisuje punktowo. Najważniejsze:
- **Descope talerzy** (`02fa08e`) zapisany; gdyby wróciła sztanga → tylko warunkowo `equipment==="barbell"`.
- **Reguła:** funkcje przez „kto/kiedy", nie tylko „co". RPE domyślnie ukryte + tooltip.
- **Reguła przekrojowa:** walidacja/limity inputów liczbowych (koniec 2222 kg).
- **Repriorytet → pierwsza klasa:** heatmapa-sylwetka (mamy `sets-per-muscle`), kalendarz/streak, „last set" inline per wiersz.
- Audyty `product-audit`/`ux-audit-mobbin` są częściowo nieaktualne (sporo P1–P5 już zrobione) — patrz addendum §6.

## Gdzie jesteśmy
MVP + rozszerzenia gotowe i działające lokalnie. Zweryfikowane realnym E2E (Claude Preview):
login, home, logger (✓→rest, podmiana, supersety, notatki, live pasek), historia, postępy,
ciało (ze zdjęciami), szczegóły ćwiczenia. Zero błędów runtime.

Zrobione fazy: 0 (fundament) · 1 (logger) · 2 (głębia) · 2.5 (offline) · 3 (builder/presety/ciało)
· 4 (insighty) · biblioteka programów · zdjęcia ciała · polish P1–P5 (logger/home/postępy/ćwiczenie/onboarding).
Audyty: `docs/product-audit.md`, `docs/ux-audit-mobbin.md`, `docs/usability-audit.md`.

## Pre-deploy P0/P1 — ZROBIONE (commity `8e674eb` + `7a5b767`)
Wszystkie uzgodnione punkty domknięte (ta lista była wcześniej nieaktualna):
1. ✅ **Dane testowe wyczyszczone** (16 sesji/24 PR/1 pomiar/1 zdjęcie) — świeży start.
2. ✅ **5 punktów P0/P1 z `usability-audit.md`:**
   - ✅ `maximumScale`/`userScalable` usunięte z `viewport` w `app/layout.tsx` (WCAG zoom),
   - ✅ dark mode przez `prefers-color-scheme` → `app/globals.css:107`,
   - ✅ `focus-visible` na ikonowych przyciskach — globalna reguła `button/a/[role=button]:focus-visible`
     w `app/globals.css:131-136` (pokrywa SetRow ✓/✕, builder ↑/↓, rest ±; brak klikalnych nie-buttonów),
   - ✅ confirm „Zakończyć trening?” przy niezaliczonych seriach (`Logger.handleFinish`, `Logger.tsx:271`),
   - ✅ undo-toast „Cofnij” po usunięciu serii/ćwiczenia (`Logger.tsx:250`, sonner).

## Potem: Phase 10 — deploy (wymaga kont użytkownika)
- Supabase cloud: nowy projekt → `supabase link` → `supabase db push` (migracje) → seed + bootstrap usera (service-role z env produkcyjnego).
- Vercel: import repo, env `NEXT_PUBLIC_SUPABASE_URL`/`ANON_KEY` (prod), `SUPABASE_SERVICE_ROLE_KEY` tylko w env serwera, `ADMIN_EMAIL/PASSWORD`.
- HTTPS na Vercel → pełne PWA (instalacja, SW offline, wibracje) — dziś po http LAN to nie działa.
- Po-deploy opcje: PL tłumaczenia instrukcji, heatmapa-sylwetka, skeletony ładowania, reorder ćwiczeń w sesji.

## Jak wznowić lokalnie (środowisko)
PATH (brew zepsuty — patrz pamięć `local-toolchain-supabase-docker`):
`export PATH="$HOME/.local/share/supabase:/Applications/Docker.app/Contents/Resources/bin:/usr/local/bin:/usr/bin:/bin:$PATH"`
1. Docker Desktop uruchomiony → `supabase start` (w `Arco app/arco`).
2. Jeśli świeża baza: `supabase db reset` → `npm run seed` → `npm run bootstrap:user`.
3. Serwer do testów: **produkcyjnie** `npm run build && npx next start -H 0.0.0.0 -p 3000`
   (dev bywa niestabilny — `config.cache=false` w dev już ustawione; jeden `next` na raz).
4. Smoke: `npm run smoke`, `npm run smoke:phase2`, `npm run smoke:offline`.
5. Telefon w tej samej sieci: `NEXT_PUBLIC_SUPABASE_URL` w `.env.local` ustawione na **LAN IP Maca**
   (sprawdź `ipconfig getifaddr en0`; było `192.168.100.16`). Wejście: `http://<LAN-IP>:3000`.
   Konto dev: `daniel.muszynski98@gmail.com` / hasło z `.env.local` (`ADMIN_PASSWORD`).

## Narzędzia/MCP
- **Mobbin** (MCP) — wymaga autoryzacji per sesja: `mcp__mobbin__authenticate` → user otwiera link → tools wstają.
- **Claude Preview** — config w `<sesja-cwd>/.claude/launch.json` (serwer „arco”, `npm run start` w katalogu arco, port 3000).

## Build/commit
- Jeden `npm run build` na raz (podwójny psuł `.next`). Po dep-changes w dev: `rm -rf .next`.
- Po zmianach UI restart produkcyjnego serwera. Commituj po działającej zmianie.
